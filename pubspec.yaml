name: erp_painel_delivery
description: Software para gerenciamento de pedidos da Central de Distribuição - Ao Gosto Carnes - Unidade Matriz

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.6.1

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations: # 👈 necessário para traduzir DatePicker e demais widgets
    sdk: flutter
  google_fonts: ^4.0.5
  shared_preferences: ^2.2.3
  http: ^1.2.1
  intl: ^0.20.2
  pdf: ^3.10.1
  path_provider: ^2.0.15
  provider: ^6.1.2
  open_file: ^3.2.1
  flutter_launcher_icons: ^0.14.3
  cupertino_icons: ^1.0.8
  fl_chart: ^0.68.0
  mask_text_input_formatter: ^2.9.0
  flutter_svg: ^2.0.9
  # google_maps_flutter: ^2.5.0  # Remova se não for usado
  geolocator: ^10.0.0
  url_launcher: ^6.1.0
  flutter_map: ^7.0.1  # Última versão estável
  latlong2: ^0.9.0  # Para coordenadas
  # flutter_map_tile_caching: ^13.0.0  # Opcional, para caching offline
  qr_flutter: ^4.1.0
  cached_network_image: ^3.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/icon/go-icon.png

flutter_launcher_icons:
  android:
    generate: true
  ios:
    generate: true
  macos:
    generate: true
  windows:
    generate: true
  image_path: "assets/icon/go-icon.png"
  adaptive_icon_background: "#FFFFFF"
  adaptive_icon_foreground: "assets/icon/icon2.png"
